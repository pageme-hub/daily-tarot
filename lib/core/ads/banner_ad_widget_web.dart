import 'package:flutter/material.dart';

/// BannerAdWidget 웹 스텁 — 웹에서는 빈 위젯
class BannerAdWidget extends StatelessWidget {
  const BannerAdWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
